interface Props {
  isOpen: boolean;
  closeModal: () => void;
  projectName: string;
}
const UploadVideoView = ({ isOpen, closeModal, projectName }: Props) => {
  return 0;
};

export default UploadVideoView;
